# Simple Data Processor

This project processes numerical data files and outputs summary statistics.

Supported formats:

- TXT
- CSV

The input format is specified in `config.json`.

Run the program with:
    python run.py
